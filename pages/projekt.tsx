import React from "react";
import NavBar from "../components/NavBar";

type Props = {};

const projekt = (props: Props) => {
  return (
    <>
      <NavBar />
    </>
  );
};

export default projekt;
